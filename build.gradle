plugins {
  id "org.sonarqube" version "2.6.2" apply false
}

subprojects {
  apply plugin: 'org.sonarqube'
  apply plugin: 'java'
  apply plugin: 'jacoco'

  repositories {
    jcenter()
  }
  sonarqube {
    properties {
      property "sonar.login", "admin"
      property "sonar.password", "Kapcsi1998"
    }
  }
}

